First type the `locale` command to print-out your current locale configuration. 

Also you can use `locale -a` to display a list of all available locales.

Install new locale via `sudo locale-gen nl_NL.UTF-8`

